// The code should create a new array where every number is doubled.

const numbers = [2, 4, 6, 8];

const doubled = numbers.map(num => {
    num * 2;
});

console.log(doubled);

// Expected output
// [4, 8, 12, 16]