//LINEAR
const first = numbers[0];

//LINEAR
for (const number of numbers) {
    console.log(number);
}

//QUADRATIC
for (const a of numbers) {
    for (const b of numbers) {
        console.log(a, b);
    }
}